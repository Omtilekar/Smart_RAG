[pytest]
testpaths = tests
markers =
    local_data: requires this project's frozen local datasets (data/)
    gpu: requires a CUDA-capable GPU
    model: requires an embedding model already present in the local Hugging Face cache (no network download)
